#include "main.h"
/**
 * add - a function that add two integers and return the result
 * @a: first parameter
 * @b: second parameter
 * Return: Allow success
 */
int add(int a, int b)
{
	return (a + b);
}
